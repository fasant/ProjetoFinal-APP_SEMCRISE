<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
         <item
            android:id="@+id/nav_ordem_preco"
            android:icon="@drawable/ic_ordem_preco"
            android:title="Listar por Preço" />
        <item
            android:id="@+id/nav_ordem_id"
            android:icon="@drawable/ic_ordem_id"
            android:title="Listar por Id" />
        <item
            android:id="@+id/nav_ordem_local"
            android:icon="@drawable/ic_ordem_local"
            android:title="Listar por Local" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_deletar"
                android:icon="@drawable/ic_delete"
                android:title="Deletar" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>

</menu>
